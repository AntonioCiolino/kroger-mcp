# FastMCP framework (2.14.3+ required for output_schema support)
fastmcp>=2.14.3

# Kroger API client
kroger-api>=0.2.0

# Additional dependencies (if not already included in kroger-api)
requests
pydantic
python-dotenv

# Web UI dependencies
flask
